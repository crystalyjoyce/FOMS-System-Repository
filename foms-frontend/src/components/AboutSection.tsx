import React from 'react';

const AboutSection = () => (
  <section className="about" id="about">
    <h2>About This Template</h2>
    <p>This frontend-only interface uses Vite and TypeScript with plain CSS for easy deployment and customization.</p>
  </section>
);

export default AboutSection;
